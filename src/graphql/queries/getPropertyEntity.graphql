query GetPropertyEntity($propertyId: Int!, $year: Int!) {
	getPropertyEntity(propertyId: $propertyId, year: $year) {
		property {
			id
			name
			address
			year
			year
		}
		rooms {
			id
			name
			available
			year
			updates {
				id
				timestamp
				room
			}
		}
	}
}
